import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    root: {
        fontSize: '18px',
        fontStyle: 'normal',
        fontWeight: 800,
        lineHeight: '27px',
        letterSpacing: '0em',
    }
}))

export default useStyles